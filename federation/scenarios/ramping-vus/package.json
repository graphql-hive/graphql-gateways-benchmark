{
  "name": "fed-ramping-vus",
  "description": "This scenario runs 4 subgraphs and a GraphQL gateway with Federation spec, and runs a heavy query. We are running a heavy load of concurrent VUs to measure response time and other stats, during stress. It measure things like memory usage, CPU usage, response times. It also includes a summary of the entire execution, and metrics information about HTTP execution times.",
  "scripts": {
    "generate-report": "tsx generate-report.ts",
    "postinstall": "patch-package"
  },
  "dependencies": {
    "vega": "6.1.2",
    "vega-lite": "6.2.0",
    "tablemark": "3.1.0",
    "tsx": "4.20.3",
    "typescript": "5.8.3",
    "graphql": "16.11.0",
    "patch-package": "8.0.0"
  },
  "devDependencies": {
    "@types/node": "22.16.5"
  }
}
